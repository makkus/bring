info:
  slug: Single-file binaries

source:
  type: template-url
  url: "https://gitlab.com/bring-indexes/binaries/-/raw/{{ version }}/binaries.br.idx"

  template_vars:
    version:
      - "master"
      - "develop"

  args:
    version:
      default: "master"

  artefact:
    type: file
